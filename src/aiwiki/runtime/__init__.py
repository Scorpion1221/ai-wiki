"""Curation runtime — triggers a sandboxed Codex agent to curate bundle concepts.

The engine and read service are deterministic. Codex supplies prose and judgment in an
isolated workspace; the service owns evidence bytes, validation, closeout, and Git.
"""
